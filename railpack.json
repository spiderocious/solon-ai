{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20",
    "pnpm": "9.15.9"
  },
  "caches": {
    "pnpm-store": {
      "directory": "/root/.local/share/pnpm/store",
      "type": "shared"
    }
  },
  "steps": {
    "install": {
      "commands": [
        "corepack enable",
        "pnpm config set store-dir /root/.local/share/pnpm/store",
        "pnpm install --frozen-lockfile"
      ],
      "caches": ["pnpm-store"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["pnpm nx build solon-demo-api"]
    }
  },
  "deploy": {
    "startCommand": "node apps/backend/solon-demo-api/server.js"
  }
}